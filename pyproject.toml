[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools >= 65.5.0",
  "wheel"
]

[tool.bandit]
exclude_dirs = ["/tests/", "/venv/"]

[tool.pylint.messages_control]
disable = """
C0103
"""
